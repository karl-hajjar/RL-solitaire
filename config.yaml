Files:
  checkpoints_dir: 'checkpoints/'
  tensorboard_log_dir: 'tensorboard/'
  results_log_path: 'results.log'

Network:
  name: 'actor-critic'
  use_bias: false
  bias_init_const: 0.0008
  grad_clip_norm: 5.0     # norm of gradients to clip
  n_filters: 64
  activation: 'elu'
  state_embedding_size: 128
  state_channels: 3
  lr: 1.0e-3
  actor_coeff: 0.4
  critic_coeff: 1.0
  reg_coeff: 1.0e-2
  value_activation: 'identity'
  gamma: 1.0

Training:
  init_checkpoint: 0      # 0 for training from scratch, any number > 0 for restoring the model from the last checkpoint 
  n_iter: 70
  n_games: 16            
  n_workers: 8
  display_every: 5

Evaluation:
  n_games: 30
  n_workers: 10